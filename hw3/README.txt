Justo Diaz Esquivel
CS450 HW3 -- WAR Server/Client Card Game
Multiplexing with select()
****************************************
Programs were written in C++. To compile in Linux, make sure g++ is installed.
You can run 'make' or 'make server' to compile the server executable.
You can also run './server.sh <port>' to compile server executable and run the server on <port>. 
